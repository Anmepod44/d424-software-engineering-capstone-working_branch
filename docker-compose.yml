services:
  # Spring Boot Application (H2 in-memory database, no PostgreSQL needed)
  app:
    build: .
    container_name: taskmanagement-app
    ports:
      - "8080:8080"
    environment:
      SPRING_PROFILES_ACTIVE: dev
    restart: unless-stopped
